{
  "name": "opencode-anthropic-proxy",
  "version": "1.0.0",
  "description": "OpenAI 兼容 API → Anthropic Messages API 反向代理",
    "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.95.0"
  }
}
